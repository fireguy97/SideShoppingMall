import React from "react";
import Layout from "../../../layout/Layout";
import TopsList from "./TopsList";

const Tops = () => {
  return (
    <Layout>
      <TopsList />
    </Layout>
  );
};

export default Tops;
